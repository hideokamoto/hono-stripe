{
  "name": "hono-stripe-example-cloudflare-workers",
  "private": true,
  "type": "module",
  "description": "Hono + Cloudflare Workers + Stripe starter: hono-stripe (intent/session + webhook) and a stripe-pwa-elements payment UI served via hono/jsx.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.12.25",
    "hono-stripe": "workspace:*",
    "stripe": "^22.2.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260610.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.20.0"
  }
}
